<%= render 'service/nav' %>

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                        <h5 class="header"><b><i class="small material-icons">live_help</i>오아미스 자주하는 질문</b></h5><br>
                        
                        <ul class="collapsible" data-collapsible="expandable">
                            
                            <% Board.where(level:1).each do |b|%>
                                <li>
                                  <div class="collapsible-header"><i class="material-icons">check_box</i><%= b.title %></div>
                                  <div class="collapsible-body"><%= simple_format b.content.gsub(/\n/, '<br/>') %></div>
                                </li>
                            <% end %>
                        </ul>
                
                
            </div>
        </div>
    </div>


<%= render 'service/footer' %>
<%= render 'service/scrt' %>